---
pagination:
  data: multipleLinks
  size: 1
  alias: item
permalink: "{{ item.permalink }}"
---
<!doctype html>
<meta charset="utf-8">
<title>{{ item.title }}</title>
<meta name="viewport" content="width=device-width">
{% for icon in item.icons %}
<link
  {% if icon.rel %}rel="{{icon.rel}}"{% else %}rel="icon"{% endif %}
  {% if icon.type !== null %}type="{{icon.type}}"{% endif %}
  {% if icon.sizes !== null %}sizes="{{icon.sizes}}"{% endif %}
  href="{{icon.iconPath}}"
>
{% endfor %}
<style>
figure {
  margin-inline: 0;
}
footer {
  margin-top: 3em;
  padding: 1em 0 2em;
  border-top: thin solid #999;
}
</style>
<body>
<h1>{{ item.title }}</h1>

{% for icon in item.icons %}
<dl>
<dt>icon
<dd>
  <a href="{{ icon.iconPath }}">{{ icon.iconPath }}</a>
  <figure>
    <img src="{{ icon.iconPath }}">
  </figure>
<dt>rel
<dd>
  {% if icon.rel %}
    <code>rel="{{icon.rel}}"</code>
  {% else %}
    <code>rel="icon"</code>
  {% endif %}
<dt>type
<dd>
  {% if icon.type === null %}
    not set
  {% elif icon.type === "" %}
    blank (<code>""</code>)
  {% else %}
    <code>type="{{icon.type}}"</code>
  {% endif %}
</dl>
{% endfor %}

<footer>
<nav>
<p><a href="/">home</a></p>
</nav>
</footer>
